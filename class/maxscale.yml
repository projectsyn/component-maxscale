parameters:
  kapitan:
    compile:
      - input_paths:
          - maxscale/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - maxscale/component/main.jsonnet
        input_type: jsonnet
        output_path: ${_instance}
